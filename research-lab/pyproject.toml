[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cliniga-thesis-research-lab"
version = "0.1.0"
description = "Evidence, citation and PRISMA validation for CliniGA Education"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

