# Curated YARA rules

Place reviewed, organization-approved YARA rules in this directory.

Rules are used only for static analysis of files inside the authorized CliniGA AI workspace. Do not add rules that execute external programs, fetch remote content, or depend on unreviewed generated payloads. Keep rule provenance and license information alongside any imported rule set.
