FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential git \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
COPY ai/requirements.txt ai/requirements-domain.txt ai/requirements-research.txt ai/requirements-clinical.txt ai/requirements-biomed-heavy.txt /tmp/cliniga/
RUN pip install -r /tmp/cliniga/requirements.txt \
    -r /tmp/cliniga/requirements-domain.txt \
    -r /tmp/cliniga/requirements-research.txt \
    -r /tmp/cliniga/requirements-clinical.txt \
    -r /tmp/cliniga/requirements-biomed-heavy.txt

COPY . /workspace
CMD ["uvicorn", "ai.app.main:app", "--host", "0.0.0.0", "--port", "8000"]
